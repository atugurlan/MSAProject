import { Text, View, TouchableOpacity } from 'react-native';
import { styles } from './styles';

export default function SeeConversationPage({ navigation }) {
    return (
        <View>
            <Text>See Conversation Page</Text>
        </View>
    );
}
