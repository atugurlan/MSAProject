import { Text, View, TouchableOpacity } from 'react-native';
import { styles } from './styles';

export default function AddQuestionPage({ navigation }) {
    return (
        <View>
            <Text>Add Question Page</Text>
        </View>
    );
}
